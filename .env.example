# Copy to .env and adjust as needed.

# HTTP port the API listens on.
PORT=3000

# DATABASE_URL is intentionally omitted — persistence (PostgreSQL + Prisma)
# is deferred. The app currently uses an in-memory adapter.
